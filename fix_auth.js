const fs = require('fs'); let c = fs.readFileSync('src/app/auth/page.tsx', 'utf8'); c = c.replace(/await signIn\('credentials', \{[\s\S]*?\}\);/, const res = await signIn('credentials', { email, password, redirect: false });\n      if (res?.error) { setError('Authentication failed.'); setLoading(false); }); c = c.replace(/await signIn\('google', \{[\s\S]*?\}\);/, wait signIn('google', { redirect: true, callbackUrl: '/' });); fs.writeFileSync('src/app/auth/page.tsx', c);
